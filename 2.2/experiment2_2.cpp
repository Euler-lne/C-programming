#include "matrix.h"
#include <bits/stdc++.h>
using namespace std;
void InputMatrix(Matrix);

int main()
{
    Matrix A(2, 4);
    A.ShowMatrix();
    return 0;
}
void InputMatirx(Matrix _matrix)
{
    for (int i = 0; i < _matrix.GetLines(); i++)
    {
        for (int j = 0; j < _matrix.GetRows(); j++)
        {
        }
    }
}